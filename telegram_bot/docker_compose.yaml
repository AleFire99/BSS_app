version: '3'

services:
  bss-bot:
    container_name: bss-bot
    image: alefire/bss-bot:latest
    restart: always
    environment:
      - BOT_TOKEN=REDACTED_BOT_TOKEN  # You can pass environment variables here if necessary